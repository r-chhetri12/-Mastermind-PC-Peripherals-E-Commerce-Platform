@* @model List<MasterMind.Models.Order>

<h2>All Orders</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>User</th>
            <th>Amount</th>
           
            <th>Order Date</th>
           
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.Id</td>
                <td>@order.Name (@order.ApplicationUser.Email)</td>
                <td>₹@order.TotalAmount</td>
                
                <td>@order.OrderDate.ToString("dd-MM-yyyy HH:mm")</td>
                
                <td>
                    <a href="@Url.Action("OrderDetails", "Order", new { id = order.Id })" class="btn btn-primary">View</a>
                </td>
            </tr>
        }
    </tbody>
</table>
 *@
@model List<MasterMind.Models.Order>
<body>

<div class="container-fluid px-4">
    <h2 class="my-4 text-center text-primary">🛒 All Orders</h2>
        <input type="text" id="productSearch" class="form-control w-25" placeholder="🔍 Search by User Name..." />

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover align-middle text-center">
                    <thead class="table-primary">
                        <tr>
                            <th scope="col">Order ID</th>
                            <th scope="col">User</th>
                            <th scope="col">Amount</th>
                            <th scope="col">Order Date</th>
                            <th scope="col">Status</th>
                            <th scope="col">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var order in Model)
                        {
                            <tr>
                                <td>@order.Id</td>
                                <td>
                                    <div>
                                        <strong>@order.Name</strong><br />
                                        <small class="text-muted">@order.ApplicationUser.Email</small>
                                    </div>
                                </td>
                                <td class="text-success fw-bold">₹@order.TotalAmount</td>
                                <td>@order.OrderDate.ToString("dd-MM-yyyy hh:mm tt")</td>
                                <td>
                                    <span class="badge rounded-pill
                                        @(order.OrderStatus == "Shipped" ? "bg-success" :
                                          order.OrderStatus == "Pending" ? "bg-warning text-dark" :
                                           order.OrderStatus == "Out for Delivery" ? "bg-info text-dark" :
                                          "bg-secondary")">
                                        @order.OrderStatus
                                    </span>
                                </td>
                                <td>
                                    <div class="d-flex justify-content-center gap-2 flex-wrap">
                                        <a href="@Url.Action("OrderDetails", "Order", new { id = order.Id })" class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-eye"></i> View
                                        </a>

                                        @if (order.OrderStatus == "Pending")
                                        {
                                            <form method="post" asp-action="ShipOrder" asp-controller="Order" class="d-inline">
                                                <input type="hidden" name="orderId" value="@order.Id" />
                                                <button type="submit" class="btn btn-sm btn-outline-success">
                                                    <i class="fas fa-shipping-fast"></i> Ship
                                                </button>
                                            </form>
                                        }
                                        else if (order.OrderStatus == "Shipped")
                                        {
                                            <form method="post" asp-action="MarkOutForDelivery" asp-controller="Order" method="post" class="d-inline">
                                                <input type="hidden" name="orderId" value="@order.Id" />
                                                <button type="submit" class="btn btn-sm btn-warning text-white">
                                                    <i class="fas fa-truck-moving"></i> Out for Delivery
                                                </button>
                                            </form>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

    <script>
        document.getElementById("productSearch").addEventListener("keyup", function () {
            const filter = this.value.toLowerCase();
            const rows = document.querySelectorAll("table tbody tr");

            rows.forEach(row => {
                const productName = row.querySelector("td:nth-child(2)")?.textContent.toLowerCase() || "";
                row.style.display = productName.includes(filter) ? "" : "none";
            });
        });
    </script>
</body>